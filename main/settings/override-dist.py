# This setings file will never be loaded, but you can copy it to override.py to apply the overrides defined in the
# dictionary below in a dev or test environment. This is so that we can test things out in dev or test
# without altering the default dev tracked settings files.

overrides = {
}
